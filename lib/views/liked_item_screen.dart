import 'package:flutter/material.dart';

class LikedItemScreen extends StatelessWidget {
  const LikedItemScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
